# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に沿って記録し、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [1.0.1] - 2026-08-26

### 追加

- 起動時の設定検証を3点追加した。いずれも不正なら起動時にエラーで停止するため、`bin/daemonctl log` で
  設定ミスに即座に気づける（既定値へ黙ってフォールバックしない）。
  - 出勤の打刻締切（`work.clock_in` + `clock_in_window`）が `work.clock_out` 以降になる設定を拒否する。
    実測では退勤が出勤より先に来て、退勤が「未出勤」で冪等スキップされ出勤だけが記録される状態になった。
  - 打刻目標が翌日にはみ出す設定（例: `clock_out: 23:59` + ウィンドウ 5分 → 翌日 00:04）を拒否する。
    日付が変わるとデーモンは前日の計画を破棄するため、この目標は必ず未打刻になる。
  - `token.refresh_threshold_days` が整数でない（値なし・小数・数値でない文字列）、または 0〜31 の範囲外の
    設定を拒否する。YAML に値なしで書くと nil が入り、打刻直前のトークン判定が毎 tick 失敗して
    grace 超過で未打刻になっていた。

### 変更

- 日中の再取得・退勤直前チェックで全休（休暇で勤務時間がなくなった）へ切り替わった場合、ログだけでなく
  Slack にも通知するようになった（同日1回）。打刻済みの分は AKASHI で手動削除が必要なため、ログを
  常時見ない運用では気づけなかった。計画時点で全休の日は従来どおり通知しない。

### 修正

- 打刻の直前に日付が変わった場合、打刻せず未打刻として扱うようにした（翌朝の「未打刻のまま日付が
  変わりました」で通知される）。打刻は壁時計の日付を対象日として AKASHI に問い合わせるため、
  日付が変わった後に打刻すると翌日を対象日として冪等スキップされ（翌日には出勤の記録がないので
  退勤が「未出勤」扱いになる）、スキップと成功を区別していなかったため通知もないまま退勤が
  失われていた。AKASHI は受信時刻で記録するので日付だけ前日に差し替えても記録日は直せず、
  「跨いだら打刻しない」が正しい。目標が 23:59 台の日に退勤直前の sukesan 再取得が遅延すると起きる。
  併せて POST の期限を「目標 + grace」と計画日の終端（日付が変わる直前）の早い方に丸め、冪等チェックの
  通信中にスリープして復帰が翌日になった場合も打刻が成立しないようにした。
- 打刻を断念（スキップ通知）する際の「既に打刻済みか」の確認を、壁時計の日付ではなく計画日について
  行うようにした。日付を跨いだ後にこの判定へ入ると翌日の打刻履歴を見てしまい、翌日に何らかの打刻
  （0時台の手動打刻など）があるとスキップ通知が抑止されたうえで計画が完了扱いになり、前日の未打刻が
  「未打刻のまま日付が変わりました」でも拾われず黙って消えていた。日付を跨がない通常時は同じ日付を
  見るため挙動は変わらない。
- sukesan の応答の形式を検証し、不一致なら取得失敗として扱うようにした。HTTP 200 でも `events` が null・
  キー欠落・配列以外・要素がオブジェクト以外という応答はあり得る。null とキー欠落は「予定なし」の成功として
  扱われて休暇情報が空で上書きされ（休暇日の防御が静かに外れる）、要素が不正な場合は定期再取得のたびに
  例外になって打刻の判定に到達せず、通知もないまま当日の打刻が止まっていた。検証はイベント内部の
  フィールドにも及び、`title` / `starts_at` / `ends_at` が nil でも文字列でもない応答、`all_day` が
  nil でも真偽値でもない応答も取得失敗として扱う（数値の `starts_at` は日時パースで、数値の `title` は
  休暇キーワード判定で、同じ無通知の停止を起こしていた）。`starts_at` / `ends_at` は文字列でも日時として
  解析できなければ取得失敗にする（従来は黙って時刻なしのイベントになり、そのイベントが退勤の判定から
  落ちて基準が所定時刻へ巻き戻っていた。取得失敗にすれば直近の目標を維持する既存の経路に乗る）。
  さらに `starts_at` / `ends_at` はタイムゾーンオフセットが付いていることも要求する（`2026-07-10T10:00:00`
  のようなオフセットなしの日時は解析自体は通るが、ホストのタイムゾーンで解釈されるため JST 基準の
  時刻計算とずれる）。nil・空文字・空白のみは従来どおり「時刻なし」として正常に扱う。`id` と `location` は
  型に依存した処理がないため検証しない。形式不正は一過性ではないためリトライせず、既存の取得失敗経路
  （所定時刻フォールバック・連続失敗の通知）に載せる。
- カレンダー由来の退勤目標が翌日に出る日は、揺らぎを落として基準時刻（最終業務イベントの終了、
  または所定退勤時刻。休暇の時間帯に入る場合は休暇の開始へ前倒しした時刻）を目標にするようにした。退勤目標は「基準 + 揺らぎ」なので、基準が当日内でも
  揺らぎで日付を越えることがあり（実測: 23:59 終了のイベント + ウィンドウ5分では12日サンプル中8日が
  翌日になった）、日付が変わると当日の計画は破棄されるためその日の退勤は必ず未打刻になっていた。
  「当日内（23:59:59）へ収める」のでは足りない: 打刻は目標到達後の tick（既定30秒間隔）で行うため、
  目標が日付変更の直前だと発火機会がほとんど無い（23:59:59 なら tick の位相30通り中1通りしか
  打刻できないのを実測で確認。基準 23:59:00 に戻せば30通りすべてで打刻される）。基準へ落としても
  退勤基準より前に打刻されることはなく、揺らぎは日付から決まる決定論的な値のままなので
  再取得で目標がブレることもない。
- `bin/daemonctl install` がロード済みの場合に job を登録し直すようにした（bootout → bootstrap）。
  `restart` の `launchctl kickstart -k` は登録済み job のプロセスを再起動するだけで plist を読み直さないため、
  Ruby の更新で bindir が変わった場合やリポジトリを移動した場合に、古い job 定義のまま動き続けていた。
  役割は install = job 定義の再登録（plist の変更を反映）、restart = プロセス再起動（`.env` /
  `config/config.yml` の変更を反映）。
- `bin/daemonctl` の `get_pid` が `launchctl list` の非0終了で失敗し、`set -euo pipefail` 配下の
  `start` / `status` が何も表示せず終了しうる状態を修正した。PID が取れないのは扱える正常な状態なので
  空文字を返して継続する。
- 打刻時刻の揺らぎのシードがローカルタイムゾーンに依存していたのを、その日の JST 午前0時を起点に
  統一した。TZ が JST 以外の環境（旅行先で Mac の設定を変えた場合など）では、同じ日でも別の揺らぎに
  なっていた。JST 環境での値は変わらないため、これまでの打刻時刻に影響はない。

## [1.0.0] - 2026-08-24

初回リリース。

### 打刻

- マネーフォワード クラウド勤怠Plus（旧AKASHI）の公開APIへ出勤(type=11)・退勤(type=12)を打刻する。
  打刻APIは `stampedAt` を無視して受信時刻で記録するため（実機検証済み・遡り登録は不可）、
  「打刻したい時刻にAPIを呼ぶ」設計にしている。
- 打刻前に当日の在席状態（最終打刻）を確認し、既に済んでいればスキップする（手動打刻との二重登録防止）。
  日跨ぎ勤務で前営業日の退勤が当日日付に記録されていても、当日の出勤後は退勤を正しく打刻する。
- 対象日判定は平日のみ・日本の祝日スキップ（holiday_jp）。除外日・追加出勤日を設定で調整できる。

### カレンダー連動

- ローカルのカレンダーAPI sukesan（127.0.0.1 限定）から当日の予定を取得し、打刻時刻を動的に決める。
  出勤は「最初の業務イベントの開始（＝打刻締切）− 揺らぎ」、退勤は「最終業務イベントの終了（＝基準）+ 揺らぎ」。
- 会食・懇親会など退勤側の除外キーワードに一致する末尾イベント、移動・私用など出勤側の除外キーワードに
  一致する先頭イベントは、連続していてもスキップして判定する。
- 揺らぎは日付と打刻種別から決まる固定値なので、再取得のたびに目標がぶれない。
- 一定間隔で再取得し、会議の追加・キャンセル・延長に追随する（出勤・退勤とも）。退勤は打刻直前にも最終確認する。
- 通信エラーとHTTP 5xxは一過性とみなして2秒→4秒のバックオフで計3回試行し、それでも失敗する場合は所定時刻へ
  フォールバックする。日中の再取得失敗では所定時刻へ巻き戻さず、直近の目標を維持する。

### 休暇

- タイトルが休暇キーワード（既定: 休み/休暇）に部分一致するイベントを「その時間帯は勤務しない」時間帯として扱い、
  打刻締切・退勤基準をその時間帯の外へ押し出す。午前休・午後休・中抜けに対応する。
- 押し出しの結果「出勤締切 >= 退勤基準」になった日は勤務時間ゼロ＝全休として打刻しない。
  終日休暇は 00:00〜翌 00:00 の休暇としてこの経路で全休になる（全休は特別ルールではなく押し出しの帰結）。
- AKASHI は休暇申請日でも打刻を受理し、公開APIから休暇申請を読む手段もないため、POST の直前に置いた単一の
  ゲートで休暇中の打刻を中止する（出勤・退勤とも、初回もリトライも必ず通る位置）。

### 誤時刻打刻ガード

- 打刻の期限は「目標 + late_grace_minutes」。窓を超えていたら打刻せずスキップして通知する
  （スリープで寝過ごした後に誤った時刻で打刻しないため）。
- 打刻の直前に現在時刻を取り直して窓を再判定する（tick 途中のスリープで復帰後に誤判定しないため）。
- 再計画で新しい目標が既に到達不能な場合は更新せず、既存の目標を維持する（出勤・退勤とも）。
- 打刻失敗は窓内で tick 毎にリトライし、窓を超えたら理由を分けて通知する。

### 常駐・運用

- launchd で常駐する30秒 tick の単一スレッドデーモン。長い sleep をせず、Macのスリープ復帰後も正しく追随する。
- pmset の起床予約を add-only 方式で自動管理する。`cancel`/`cancelall` は使わないため、同居する他の
  pmset 利用デーモンの予約を壊さず、逆に消されても次の tick で自己回復する。
- 異常時のみ Slack に通知する（打刻の寝過ごしスキップ、リトライ枯渇、トークン再発行失敗、sukesan 障害
  フォールバック、休暇中のための打刻中止、当日計画の作成失敗、未打刻のまま日付変更）。送信に失敗した通知は
  同一プロセスのメモリに保持して以降の tick で再送する。定期再取得の失敗は連続失敗が閾値に達するまで通知しない。
- アクセストークン（有効期限は1ヶ月と1日）を期限前に自動再発行し、atomic write で保存する。
- CLI: `plan`（ドライラン表示）、`status`、`clock_in`/`clock_out`、`refresh_token`、`recheck`、`daemon`、
  `launchd`、`sudoers`、`version`。デーモンの操作は `bin/daemonctl` に集約。
- 依存は thor / holiday_jp / logger のみ（HTTP・JSON は stdlib）。副作用はすべて注入可能で、
  テストは実 sleep・実ネットワーク・実 pmset なしに動く。

### 動作環境

- macOS（launchd 常駐 / pmset 自動起床）、Ruby 3.2+
- 同一マシンで稼働するカレンダーAPI sukesan

[1.0.1]: https://github.com/namikawa/ak4-punch/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/namikawa/ak4-punch/releases/tag/v1.0.0
